# server {
#     listen 80;
#     server_name localhost;

#     root /usr/share/nginx/html;
#     index index.html;

#     # Serve static React files or fallback to index.html for React Router
#     location / {
#         try_files $uri $uri/ /index.html;
#     }

#     # Proxy /api/users to the user-service container
#     location /api/users {
#         proxy_pass http://user-service:8080/api/users;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # Proxy /api/products, /api/categories, /api/inventory to the product-service container
#     location ~* ^/api/(products|categories|inventory) {
#         proxy_pass http://product-service:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # Proxy /api/orders, /api/payments to the order-service container
#     location ~* ^/api/(orders|payments) {
#         proxy_pass http://order-service:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }


server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}