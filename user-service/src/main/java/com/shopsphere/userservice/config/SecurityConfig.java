package com.shopsphere.userservice.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
    // Security dependencies such as spring-boot-starter-security are not yet added.
    // This will hold security configurations once included in the POM.
}
